# AGENTS.md

> PromptVault 项目级 Agent 开发规范。适用于仓库根目录及全部子目录；如果某个子目录存在更具体的 `AGENTS.md`，则该子目录同时遵循更具体的约束。

## 1. 产品目标与 v0.1 边界

PromptVault 是公开访问的 Prompt 展示与使用站点，首版核心路径固定为：

`Explore → Detail → Customize → Copy`

首版包含公开浏览、分类/标签筛选、详情、Prompt 定制与复制、中文/英文、后台必要 CRUD、图片上传、SSR、SEO，以及桌面端和移动端响应式体验。

首版不加入以下能力，除非当前任务由用户明确改变范围：

- 用户注册、登录及前台账户体系。
- 收藏、点赞，包括匿名本地收藏。
- 用户投稿、评论。
- 在线生成图片。
- 向量搜索。
- 个人主页、关注等账户衍生能力。
- 移动端底部四入口导航。

不得因为 Stitch 设计历史元素、第三方模板能力或 Agent 自主判断而扩大产品范围。

## 2. 事实源与优先级

开始任务前，按以下顺序确认约束：

1. 当前任务中用户明确给出的目标、边界和验收要求。
2. 本文件中的产品与工程约束。
3. 根目录 `DESIGN.md` 中的全局设计系统。
4. `docs/ui/` 中与当前页面对应的 Stitch 设计产物。
5. 当前代码库已经形成的稳定实现模式。

设计职责：

- `DESIGN.md` 负责颜色、字体、间距、组件原则、响应式、国际化等全局规则。
- `docs/ui/` 负责页面结构、信息层级、组件组合和具体视觉参考。
- UI 任务必须同时阅读两者。
- `docs/ui/` 默认只作为设计源，不得为了实现页面而修改其中的 Stitch 产物。
- 当页面设计与全局设计规范冲突时，优先保持 `DESIGN.md` 的系统一致性，并在交付说明中记录差异。
- 不允许脱离设计稿自行重新设计页面。

## 3. 已冻结技术路线

除非当前任务明确属于架构调整，否则保持以下路线：

- 单仓库。
- 单 Cloudflare Worker。
- TypeScript。
- React Router v8 SSR。
- Vite。
- Cloudflare Workers 作为服务端运行环境。
- Cloudflare D1 保存业务元数据与内容。
- Cloudflare R2 保存图片等对象资源。
- 不使用 ORM。
- 不引入 Python Worker、FastAPI 或独立 API Server。

域名：

- 主站：`https://vault.disign.me`
- 图片：`https://vault-pic.disign.me`

图片资源优先通过图片域名直接访问，避免无必要地经过主 Worker 中转。

所有依赖必须确认能够运行于 Cloudflare Workers。避免依赖本地原生二进制、传统长驻 Node.js Server、文件系统、后台线程等运行时假设。确需 Node.js 兼容能力时，先检查现有 Wrangler 配置，不得顺手扩大兼容层。

## 4. Prompt 变量契约

变量语法固定为：

`{{key}}`

`key` 是稳定业务标识，不使用展示文案作为 key。

首版变量类型只有：

- `text`
- `select`

变量默认可选。复制 Prompt 时必须满足：

- 未填写：保留原始 `{{key}}`。
- 已填写：替换该 key 的全部对应占位符。
- 同一 key 多次出现时使用同一个值。
- 替换只执行一轮。
- 用户输入即使包含新的 `{{other_key}}`，也不得递归展开。
- 用户填写值只保存在当前浏览器交互状态，不建立账户级或服务端持久化。

如果 Prompt 没有变量：

- 不展示空的 Customize 表单。
- 直接提供一键复制。
- 上传图片型等无占位符 Prompt 同样遵循此规则。

管理端保存或发布 Prompt 时，应根据 Prompt 内容扫描并校验变量定义，至少保证变量可识别、key 唯一、`select` 具有合法选项，并且前端不得依赖展示文案猜测变量类型。已有数据模型定义更严格规则时，以现有契约为准。

## 5. 国际化

首版正式支持：

- `zh-CN`
- `en-US`

其他语言当前不提供正式内容，但实现必须保留扩展空间。

要求：

- 通用界面文案进入统一国际化层，不在组件中散落硬编码。
- 业务枚举保存稳定 key，展示文本通过国际化映射生成。
- 中文和英文内容按现有数据模型组织，不复制两套业务结构。
- 布局能够容纳更长的英文文本，避免依赖固定字符宽度。
- 不通过字符串拼接构造复杂翻译句子，优先使用完整翻译单元。
- 日期、数字等格式化使用 locale-aware 标准能力。
- 不写死会阻碍后续更多语言或不同文字长度的布局规则。

## 6. SSR、数据访问与 SEO

PromptVault 是内容型站点，SSR 与 SEO 属于基础能力。

### SSR

- Explore 与 Detail 的核心可索引内容必须由 SSR 输出。
- 不得把主要 Prompt 内容改成浏览器加载后才出现的纯客户端内容。
- 首屏数据优先使用 React Router 的服务端数据加载机制。
- 浏览器不得直接访问 D1。

### D1

- Worker 服务端直接访问 D1。
- 查询只选择页面需要的字段，避免明显 N+1。
- 索引围绕真实筛选、排序和关联查询设计。
- 不使用 ORM 隐藏 SQL。
- migration 通过明确的迁移流程执行，不得在普通业务请求中自动跑数据库迁移。

### R2

- 对象 key 使用稳定规则，并注意大小写敏感。
- 图片输出明确尺寸或稳定宽高比，降低布局偏移。
- 非首屏关键图片按场景延迟加载。
- 不为了接口形式统一而让所有图片经过主 Worker。

### SEO

详情页保持可索引服务端 HTML、正确 title/description、稳定 URL、canonical 和当前语言对应的元信息。项目已有 `hreflang`、结构化数据、站点地图等能力时，延续现有方案。

## 7. UI 开发规则

UI 任务开始编码前，至少阅读：

- 根目录 `DESIGN.md`。
- `docs/ui/` 中相关设计文件。
- 当前页面实现。
- 相关公共组件。

实现目标是把 Stitch 设计转成可维护的生产代码，保留信息层级、页面骨架、主要间距、字体层级、图片比例、关键交互位置和桌面/移动结构关系，同时优先复用项目已有设计 token 与组件，避免大量页面级魔法数。

当前移动端首版重点覆盖 Explore 与 Detail，并且：

- 不增加底部四入口导航。
- 不增加收藏入口。
- 不增加登录/注册入口。
- 不因为屏幕变窄而删除 Prompt、变量填写或复制等核心能力。

组件开发要求：

- 同一视觉语义优先复用同一个组件。
- 新增公共组件前先搜索现有代码。
- 不复制已有组件后仅做轻微修改。
- 不为一次性细节制造过度抽象框架。
- loading、empty、error、copied 等状态沿用现有视觉系统。

可访问性要求：

- 使用语义化 HTML。
- 操作使用正确的 button/link 语义。
- 关键流程支持键盘。
- 保留明确 focus 状态。
- 图标按钮提供可访问名称。
- 图片根据用途设置正确 alt。
- 不把颜色作为唯一状态信号。

## 8. Windows 与跨平台约束

后续主要由 Windows Agent 推进，因此代码和脚本必须保持跨平台：

- 项目脚本不依赖 Bash 专属语法。
- 文件路径使用标准路径 API，不手工拼接 Windows 反斜杠。
- Web 路由、URL、资源路径使用 URL 语义。
- 不引入只能在 macOS/Linux 工作的构建步骤。
- PowerShell 可用于本地诊断，但不应成为跨平台项目的隐性依赖。
- 文本文件使用 UTF-8。
- 注意 Windows 文件系统大小写不敏感与线上环境的差异。

## 9. Agent 工作流

### 开工前

1. 读取当前目录适用的 `AGENTS.md`。
2. 查看 `git status`，识别用户已有修改。
3. UI 任务读取 `DESIGN.md` 与对应 `docs/ui/`。
4. 阅读相关 route、component、loader/action、schema、migration 和测试。
5. 从 lockfile、`package.json` 与现有 scripts 确认包管理器和验证命令。
6. 确定本次任务的最小修改集合。

不得覆盖、回滚或清理用户已有未提交修改。

### 实现中

- 优先最小且完整的变更。
- 一个任务只处理与验收目标直接相关的范围。
- 不顺手做大规模重构或批量格式化。
- 不无理由升级依赖。
- 新增依赖前先确认现有依赖或 Web 标准能力是否足够。
- 业务规则集中在可测试位置，避免散落多个组件。
- 不因 UI 实现方便而改变数据库模型、公开 URL 或产品范围。

### 验证

不要猜测命令名，先读取 `package.json`。按照仓库已有 scripts 执行与变更相关的 format、lint、typecheck、test、build。

UI 任务还需要检查：

- 桌面端与移动端。
- `zh-CN` 与 `en-US`。
- loading / empty / error。
- 长标题、长 Prompt、长英文。
- 变量填写与复制。
- 无变量 Prompt 的直接复制。
- SSR 页面源中包含核心内容。

变量相关代码至少覆盖：

1. `text`。
2. `select`。
3. 同一 key 多次出现。
4. 部分填写、部分留空。
5. 全部留空。
6. 输入值再次包含 `{{...}}`。
7. 完全无变量。
8. 复制结果与页面预览一致。

### 交付

完成后只报告与本次任务相关的信息：

- 修改文件。
- 用户可见行为。
- 验证命令与结果。
- 真实存在的限制或阻塞。

## 10. Git 纪律

- 修改前后检查 `git status`。
- 不使用会破坏用户工作区的 `reset --hard`、强制 checkout、clean 等操作。
- 不覆盖无关未提交修改。
- 默认不自动 commit 或 push，除非当前任务明确要求。
- 需要提交时，只提交当前任务相关文件。
- commit message 描述最终有效变化。
- 不提交临时调试文件、构建产物、本机配置。
- 不修改 `docs/ui/`，除非任务明确要求更新设计源。

## 11. 架构变更边界

普通功能任务不得顺手执行以下变化：

- 从单 Worker 拆成多个后端服务。
- 引入新的服务端语言或运行时。
- 引入 ORM。
- 替换 React Router SSR。
- 更换 D1 或 R2。
- 新增前台账户体系。
- 新增收藏、评论、投稿、在线生图、向量搜索。
- 改变 Prompt 占位符语法。
- 改变主域名或图片域名。
- 大规模重做目录结构或状态管理。

如果任务确实需要其中某项，应将其作为独立架构决策处理。

## 12. 完成条件

任务完成必须同时满足：

- 当前用户目标已实现。
- 未扩张 v0.1 产品范围。
- UI 与 `DESIGN.md`、对应 `docs/ui/` 一致。
- 桌面和移动端没有明显响应式回归。
- 中文和英文可正常工作。
- SSR 与核心 SEO 未退化。
- Prompt 变量行为符合本文件契约。
- 适用的类型检查、测试、构建已通过，或准确记录真实阻塞。
- 未覆盖用户已有修改，也未混入无关文件变化。
